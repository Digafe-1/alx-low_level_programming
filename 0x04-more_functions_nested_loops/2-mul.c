#include "main.h"
/**
 * mul - Multiplcation that multiplies two integers
 * @x: The first input
 * @y: The second input
 * Return:multiplication result
 */

int mul(int x, int y)
{
	return (x * y);
}
